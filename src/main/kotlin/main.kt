import fractions.Fraction

fun main(args: Array<String>) {
    val fractions = listOf<Fraction>(
        Fraction(1,2),
        Fraction(1,-2),
        Fraction(-1,2),
        Fraction(-1,-2)
    )



}